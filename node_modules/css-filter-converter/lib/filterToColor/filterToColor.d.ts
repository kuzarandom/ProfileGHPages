import { FilterToColorOptions } from '../shared/types/options';
import { FilterToColorResult } from '../shared/types/result';
import { HEX, HSL, RGB } from 'color-convert/conversions';
export declare class FilterToColor {
    static filterToHex(filterString: string): Promise<FilterToColorResult<HEX>>;
    static filterToRgb(filterString: string, options?: FilterToColorOptions): Promise<FilterToColorResult<RGB | string>>;
    static filterToHsl(filterString: string, options?: FilterToColorOptions): Promise<FilterToColorResult<HSL | string>>;
}
