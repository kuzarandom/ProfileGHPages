import { FilterToColorResult } from '../../../shared/types/result';
import { FilterToHexShared } from './shared';
export declare class FilterToHexNode extends FilterToHexShared {
    private static readonly BASE_64_ENCODING;
    private static readonly ENCODED_DATA_URL_PREFIX;
    private static readonly IS_HEADLESS;
    private static readonly SVG_SIDE_LENGTH_PX;
    private static cleanup;
    private static returnError;
    private static getImageBase64ViaSVG;
    private static openBrowserPage;
    private static addSVGAndValidateFilter;
    private static getPuppeteerDependency;
    private static preparePuppeteerBrowser;
    static convert<T>(filterString: string): Promise<FilterToColorResult<T>>;
}
