import { Error } from '../../../shared/types/error';
export interface SVGAddResult extends Partial<Error> {
    svgContainerElement: HTMLElement;
}
export declare class FilterToHexShared {
    protected static generateInputErrorMessage(filterString: string): string;
    protected static addSVGElementsToDOMAndValidateFilter(filterString: string, svgSideLength?: number): SVGAddResult;
    protected static getColorViaImageDataURL(base64EncodedDataURL: string): Promise<string>;
}
