import { ColorToFilterOptions } from '../../types/options';
export declare class SheenUtil {
    static SHEEN_FILTER_PREFIX: string;
    static parseSheenFromOptions(options?: ColorToFilterOptions): boolean;
}
