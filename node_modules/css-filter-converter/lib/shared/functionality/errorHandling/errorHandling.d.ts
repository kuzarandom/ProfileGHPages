import { UnexpectedError } from '../../types/unexpectedError';
import { ColorFormats } from '../../consts/colorFormats';
import { ColorToFilterResult } from '../../types/result';
import { ColorTypes } from '../../consts/colorTypes';
import { Error } from '../../types/error';
export declare class ErrorHandling {
    static generateErrorResult(message: string): ColorToFilterResult<null>;
    static generateInputErrorMessage(colorType: ColorTypes, colorString: string, format?: ColorFormats): string;
    static generateUnexpectedError(error: UnexpectedError): ColorToFilterResult<null>;
    static hasError<T>(param: T | Error): param is Error;
}
